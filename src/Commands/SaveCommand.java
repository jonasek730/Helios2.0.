package Commands;
import Class.*;
public class SaveCommand implements Command{

    public String execute(String command) {
        return "";
    }

    public boolean exit() {
        return false;
    }
}
